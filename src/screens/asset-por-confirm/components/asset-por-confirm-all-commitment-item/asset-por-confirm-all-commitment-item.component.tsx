import { memo } from 'react';

type AssetPorConfirmAllCommitmentItemProps = {
  isMyAsset: boolean;
  commitment: [string, string];
};

export const AssetPorConfirmAllCommitmentItem =
  memo<AssetPorConfirmAllCommitmentItemProps>(() => {
    return null;
  });
